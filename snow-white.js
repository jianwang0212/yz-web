const snowCopy = {
    zh: {
        '.snow-hero .snow-kicker': '原创音乐作品',
        '#snow-title': '白雪公主 <span>Snow White</span>',
        '.snow-hero-cta a:nth-child(1)': '试听',
        '.snow-hero-cta a:nth-child(2)': '视觉世界',
        '.snow-hero-cta a:nth-child(3)': '资料包',
        '.snow-meta-grid div:nth-child(1) span': '演唱',
        '.snow-meta-grid div:nth-child(2) span': '作曲',
        '.snow-meta-grid div:nth-child(3) span': '作词',
        '.snow-audio-header > div:first-child span': '母带',
        '.snow-audio-actions a:first-child': '下载 MP3',
        '.snow-audio-actions a:nth-child(2)': '下载 WAV',
        '.snow-audio-actions a:nth-child(3)': 'B站合集',
        '.hero-season-card span': 'B站合集',
        '.hero-season-card small': '3 个视频 · 05-01 更新',
        '.snow-story .snow-kicker': '创作故事',
        '#story-title': '白雪、公主、白裙，与没有说出口的诺言。',
        '.story-stats div:nth-child(1) span': '份曲谱与分谱',
        '.story-stats div:nth-child(2) span': '项目预算',
        '.story-stats div:nth-child(3) span': '录音与母带',
        '.snow-notes .snow-kicker': '制作笔记',
        '#notes-title': '从情绪路径到钢琴编配。',
        '.notes-grid article:nth-child(1) h3': '情绪路径',
        '.notes-grid article:nth-child(1) p': '笔记把每个段落标成不同强度：第一段更平静，第二段逐渐增加动态，副歌和桥段把故事推到更高张力。',
        '.notes-grid article:nth-child(2) h3': '钢琴编配',
        '.notes-grid article:nth-child(2) p': '钢琴草稿强调固定动机、弱拍强化、左手切拍、经过低音、副旋律线，以及 16 分音符和三连音带来的情绪推动。',
        '.notes-grid article:nth-child(3) h3': '混音复盘',
        '.notes-grid article:nth-child(3) p': '后期笔记提醒先把 vocal、drums、piano 的基本平衡做好，再处理 reverb、compression、panning 和 guitar focal points。',
        '.snow-videos .snow-kicker': '视频合集',
        '#videos-title': '原创-白雪公主的制作',
        '.snow-videos .section-lede': 'B站合集目前有 3 个视频，更新到 2026-05-01。这个模块把每集内容按创作顺序整理出来，点击卡片可打开对应视频。',
        '.season-feature-copy span': 'Bilibili · 3 个视频',
        '.season-feature-copy strong': '从一首原创歌的灵感，到 Demo、Leadsheet、编曲层次和动态。',
        '.season-feature-copy small': '写歌、Demo 搭建、Lead Sheet、编曲层次与动态。',
        '.episode-card:nth-child(1) .episode-meta span:nth-child(3)': '110 次播放',
        '.episode-card:nth-child(1) p': '内容：从“白色”和童话意象出发，讲旋律、灵感、作词作曲最初如何成形。',
        '.episode-card:nth-child(2) .episode-meta span:nth-child(3)': '57 次播放',
        '.episode-card:nth-child(2) p': '内容：把原创想法落进 Logic 工程、Lead Sheet 和初步编曲方向，展示 Demo 是怎么搭起来的。',
        '.episode-card:nth-child(3) .episode-meta span:nth-child(3)': '150 次播放',
        '.episode-card:nth-child(3) p': '内容：讲编曲的层次、动态和乐器功能，重点不是堆叠音色，而是让歌曲推进成立。',
        '.snow-lyrics .snow-kicker': '歌词',
        '#lyrics-title': '中文歌词',
        '.lyrics-grid article[lang="zh-Hant"] h3': '中文',
        '.snow-credits .snow-kicker': '制作名单',
        '#credits-title': '制作与录音信息',
        '.credits-card:nth-child(1) h3': '核心信息',
        '.credits-card:nth-child(2) h3': '时间线',
        '.credits-card:nth-child(3) h3': '技术团队',
        '.credits-card-wide h3': '乐手与鸣谢',
        '.snow-resources .snow-kicker': '资料',
        '#resources-title': '曲谱、文档与制作文件',
        '.resource-preview span': '第一页预览',
        '.resource-preview strong': '全谱',
        '.resource-download-button': '下载完整总谱',
        '.resource-list button:nth-child(1)': '<span>加密 PDF</span> 主旋律谱',
        '.resource-list button:nth-child(2)': '<span>加密 PDF</span> 人声谱',
        '.resource-list a:nth-child(3)': '<span>PDF</span> 歌词',
        '.resource-list a:nth-child(4)': '<span>PDF</span> 制作名单',
        '.resource-list button:nth-child(5)': '<span>加密 PDF</span> 分谱',
        '.resource-list a:nth-child(6)': '<span>PDF</span> 制作笔记',
        '.resource-list button:nth-child(7)': '<span>加密 PDF</span> 钢琴编配',
        '.resource-list a:nth-child(8)': '<span>XLSX</span> 预算',
        '.resource-list a:nth-child(9)': '<span>Bilibili</span> B站合集',
        '#snow-download-form .snow-kicker': '密码下载',
        '#snow-download-title': '输入访问密码',
        'label[for="snow-download-password"]': '访问密码',
        '.snow-download-submit': '下载',
        '.snow-footer .footer-links a:first-child': '首页'
    },
    en: {
        '.snow-hero .snow-kicker': 'Original music work',
        '#snow-title': 'Snow White <span>白雪公主</span>',
        '.snow-hero-cta a:nth-child(1)': 'Listen',
        '.snow-hero-cta a:nth-child(2)': 'Visuals',
        '.snow-hero-cta a:nth-child(3)': 'Files',
        '.snow-meta-grid div:nth-child(1) span': 'Artist',
        '.snow-meta-grid div:nth-child(2) span': 'Composer',
        '.snow-meta-grid div:nth-child(3) span': 'Lyrics',
        '.snow-audio-header > div:first-child span': 'Mastered',
        '.snow-audio-actions a:first-child': 'Download MP3',
        '.snow-audio-actions a:nth-child(2)': 'Download WAV',
        '.snow-audio-actions a:nth-child(3)': 'Bilibili season',
        '.hero-season-card span': 'Bilibili Season',
        '.hero-season-card small': '3 videos · updated 05-01',
        '.snow-story .snow-kicker': 'Song story',
        '#story-title': 'Snow White, a white dress, and the promises left unsaid.',
        '.story-stats div:nth-child(1) span': 'score files and parts',
        '.story-stats div:nth-child(2) span': 'project budget',
        '.story-stats div:nth-child(3) span': 'recording and mastering',
        '.snow-notes .snow-kicker': 'Production notes',
        '#notes-title': 'From emotional path to piano arranging.',
        '.notes-grid article:nth-child(1) h3': 'Emotional path',
        '.notes-grid article:nth-child(1) p': 'The notes map each section by emotional intensity: the first verse stays calmer, the second verse grows dynamically, and the chorus and bridge push the story toward greater tension.',
        '.notes-grid article:nth-child(2) h3': 'Piano arranging',
        '.notes-grid article:nth-child(2) p': 'The piano sketch focuses on fixed motifs, offbeat emphasis, left-hand syncopation, passing bass motion, countermelody, and 16th-note or triplet motion for emotional lift.',
        '.notes-grid article:nth-child(3) h3': 'Mixing review',
        '.notes-grid article:nth-child(3) p': 'The later mixing notes return to the core balance of vocal, drums, and piano before refining reverb, compression, panning, and guitar focal points.',
        '.snow-videos .snow-kicker': 'Video season',
        '#videos-title': 'Making Snow White',
        '.snow-videos .section-lede': 'The Bilibili season currently has 3 videos, updated through 2026-05-01. Each card below links to the original episode.',
        '.season-feature-copy span': 'Bilibili · 3 videos',
        '.season-feature-copy strong': 'From a song idea to a demo, lead sheet, arrangement layers, and dynamics.',
        '.season-feature-copy small': 'Songwriting, demo building, lead sheet, arrangement layers, and dynamics.',
        '.episode-card:nth-child(1) .episode-meta span:nth-child(3)': '110 plays',
        '.episode-card:nth-child(1) p': 'Basic content: the songwriting origin, including melody, inspiration, and the first lyric/composition decisions.',
        '.episode-card:nth-child(2) .episode-meta span:nth-child(3)': '57 plays',
        '.episode-card:nth-child(2) p': 'Basic content: turning the song idea into a Logic demo, lead sheet, and concrete arrangement direction.',
        '.episode-card:nth-child(3) .episode-meta span:nth-child(3)': '150 plays',
        '.episode-card:nth-child(3) p': 'Basic content: arrangement layers and dynamics, focusing on musical function rather than simply adding instruments.',
        '.snow-lyrics .snow-kicker': 'Lyrics',
        '#lyrics-title': 'English translation',
        '.lyrics-grid article[lang="en"] h3': 'English',
        '.snow-credits .snow-kicker': 'Credits',
        '#credits-title': 'Production and recording information',
        '.credits-card:nth-child(1) h3': 'Core',
        '.credits-card:nth-child(2) h3': 'Timeline',
        '.credits-card:nth-child(3) h3': 'Technical',
        '.credits-card-wide h3': 'Musicians and thanks',
        '.snow-resources .snow-kicker': 'Resources',
        '#resources-title': 'Score, documents, and production files',
        '.resource-preview span': 'First-page preview',
        '.resource-preview strong': 'Full Score',
        '.resource-download-button': 'Download full score',
        '.resource-list button:nth-child(1)': '<span>Locked PDF</span> Lead Sheet',
        '.resource-list button:nth-child(2)': '<span>Locked PDF</span> Vocal Score',
        '.resource-list a:nth-child(3)': '<span>PDF</span> Lyrics',
        '.resource-list a:nth-child(4)': '<span>PDF</span> Credit List',
        '.resource-list button:nth-child(5)': '<span>Locked PDF</span> Charts',
        '.resource-list a:nth-child(6)': '<span>PDF</span> Production Notes',
        '.resource-list button:nth-child(7)': '<span>Locked PDF</span> Piano Arrangement',
        '.resource-list a:nth-child(8)': '<span>XLSX</span> Budget',
        '.resource-list a:nth-child(9)': '<span>Bilibili</span> Season',
        '#snow-download-form .snow-kicker': 'Protected download',
        '#snow-download-title': 'Enter access code',
        'label[for="snow-download-password"]': 'Access code',
        '.snow-download-submit': 'Download',
        '.snow-footer .footer-links a:first-child': 'Homepage'
    }
};

const snowNavCopy = {
    zh: {
        'nav.home': '首页',
        'nav.yearReview': '年度总结',
        'nav.works': '作品',
        'nav.contact': '联系',
        'nav.more': '更多',
        'nav.projects': '项目',
        'nav.engineering': '工程 / GitHub',
        'nav.timeline': '时间线',
        'nav.resume': '简历',
        'nav.finance': '财务仪表盘',
        'nav.interests': '兴趣爱好',
        'nav.highlights': '主要亮点'
    },
    en: {
        'nav.home': 'Home',
        'nav.yearReview': 'Year Review',
        'nav.works': 'Works',
        'nav.contact': 'Contact',
        'nav.more': 'More',
        'nav.projects': 'Projects',
        'nav.engineering': 'Engineering / GitHub',
        'nav.timeline': 'Timeline',
        'nav.resume': 'Resume',
        'nav.finance': 'Finance Dashboard',
        'nav.interests': 'Interests',
        'nav.highlights': 'Highlights'
    }
};

const snowDownloadLabels = {
    zh: {
        'full-score': '全谱',
        'lead-sheet': '主旋律谱',
        'vocal-score': '人声谱',
        charts: '分谱',
        'piano-arrangement': '钢琴编配'
    },
    en: {
        'full-score': 'Full Score',
        'lead-sheet': 'Lead Sheet',
        'vocal-score': 'Vocal Score',
        charts: 'Charts',
        'piano-arrangement': 'Piano Arrangement'
    }
};

function getSnowLanguage() {
    return localStorage.getItem('language') === 'en' ? 'en' : 'zh';
}

function snowText(key) {
    const lang = getSnowLanguage();
    const messages = {
        zh: {
            downloading: '下载中',
            incorrect: '密码不正确',
            failed: '下载失败，请稍后再试',
            network: '网络错误，请稍后再试',
            submit: '下载'
        },
        en: {
            downloading: 'Downloading',
            incorrect: 'Incorrect access code',
            failed: 'Download failed. Please try again later.',
            network: 'Network error. Please try again later.',
            submit: 'Download'
        }
    };
    return messages[lang][key];
}

function applySnowLanguage() {
    const lang = getSnowLanguage();
    const translations = snowCopy[lang];

    document.documentElement.lang = lang === 'zh' ? 'zh-CN' : 'en';

    document.querySelectorAll('[data-i18n]').forEach((element) => {
        const key = element.getAttribute('data-i18n');
        const value = snowNavCopy[lang][key];
        if (value) {
            element.textContent = value;
        }
    });

    document.querySelectorAll('.lang-btn, .lang-btn-mobile').forEach((button) => {
        const isActive = button.id.includes(lang);
        button.classList.toggle('active', isActive);
        button.setAttribute('aria-pressed', isActive ? 'true' : 'false');
    });

    Object.entries(translations).forEach(([selector, value]) => {
        document.querySelectorAll(selector).forEach((element) => {
            if (value.includes('<')) {
                element.innerHTML = value;
            } else {
                element.textContent = value;
            }
        });
    });

    document.querySelectorAll('[data-protected-file]').forEach((button) => {
        const key = button.dataset.protectedFile;
        if (snowDownloadLabels[lang][key]) {
            button.dataset.downloadLabel = snowDownloadLabels[lang][key];
        }
    });
}

function initSnowLanguage() {
    applySnowLanguage();
    window.addEventListener('site-language-change', applySnowLanguage);

    document.addEventListener('click', (event) => {
        const button = event.target.closest('#lang-zh, #lang-en, #lang-zh-mobile, #lang-en-mobile, .lang-btn, .lang-btn-mobile');
        if (!button) return;

        const text = button.textContent.trim().toLowerCase();
        const lang = button.id.includes('en') || text === 'english' ? 'en' : 'zh';
        event.preventDefault();
        event.stopPropagation();
        localStorage.setItem('language', lang);
        applySnowLanguage();
        window.dispatchEvent(new CustomEvent('site-language-change', { detail: { language: lang } }));
    });
}

if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', initSnowLanguage);
} else {
    initSnowLanguage();
}

const audio = document.getElementById('snow-audio-player');
const canvas = document.getElementById('snow-visualizer');

if (audio && canvas) {
    const ctx = canvas.getContext('2d');
    const AudioContextCtor = window.AudioContext || window.webkitAudioContext;
    let audioContext;
    let analyser;
    let source;
    let dataArray;
    let animationFrame;
    let idleDrawTime = 0;
    let isCanvasVisible = true;
    let seeded = 0;

    const palette = ['#ff8ab7', '#72ddff', '#eaf7ff', '#14243d'];

    function resizeCanvas() {
        const ratio = window.devicePixelRatio || 1;
        const rect = canvas.getBoundingClientRect();
        canvas.width = Math.max(1, Math.floor(rect.width * ratio));
        canvas.height = Math.max(1, Math.floor(rect.height * ratio));
    }

    function drawIdle() {
        const width = canvas.width;
        const height = canvas.height;
        ctx.clearRect(0, 0, width, height);
        for (let i = 0; i < 72; i += 1) {
            const x = (i / 71) * width;
            const base = Math.sin(i * 0.48 + seeded) * 0.28 + Math.sin(i * 0.13) * 0.2;
            const barHeight = (0.26 + Math.abs(base)) * height * 0.58;
            ctx.fillStyle = palette[i % palette.length];
            ctx.globalAlpha = 0.32;
            ctx.fillRect(x, (height - barHeight) / 2, Math.max(2, width / 120), barHeight);
        }
        seeded += 0.01;
        ctx.globalAlpha = 1;
    }

    function drawActive() {
        if (!analyser || !dataArray) return;
        analyser.getByteFrequencyData(dataArray);
        const width = canvas.width;
        const height = canvas.height;
        const bars = 88;
        const step = Math.floor(dataArray.length / bars);
        const barWidth = width / bars;

        ctx.clearRect(0, 0, width, height);
        for (let i = 0; i < bars; i += 1) {
            const value = dataArray[i * step] / 255;
            const eased = Math.pow(value, 0.82);
            const barHeight = Math.max(3, eased * height * 0.82);
            ctx.fillStyle = palette[i % palette.length];
            ctx.globalAlpha = 0.34 + eased * 0.56;
            ctx.fillRect(i * barWidth, (height - barHeight) / 2, Math.max(2, barWidth - 3), barHeight);
        }
        ctx.globalAlpha = 1;
        animationFrame = requestAnimationFrame(drawActive);
    }

    function bootAudioGraph() {
        if (audioContext || !AudioContextCtor) return;
        audioContext = new AudioContextCtor();
        analyser = audioContext.createAnalyser();
        analyser.fftSize = 256;
        dataArray = new Uint8Array(analyser.frequencyBinCount);
        source = audioContext.createMediaElementSource(audio);
        source.connect(analyser);
        analyser.connect(audioContext.destination);
    }

    function renderIdleLoop(timestamp = 0) {
        if (!audio.paused || !isCanvasVisible || document.hidden) return;
        if (!idleDrawTime || timestamp - idleDrawTime > 80) {
            drawIdle();
            idleDrawTime = timestamp;
        }
        animationFrame = requestAnimationFrame(renderIdleLoop);
    }

    resizeCanvas();
    drawIdle();

    if ('IntersectionObserver' in window) {
        const visualizerObserver = new IntersectionObserver((entries) => {
            isCanvasVisible = entries.some((entry) => entry.isIntersecting);
            cancelAnimationFrame(animationFrame);
            if (audio.paused && isCanvasVisible) {
                renderIdleLoop();
            }
        }, { threshold: 0.12 });
        visualizerObserver.observe(canvas);
    } else {
        renderIdleLoop();
    }

    audio.addEventListener('play', async () => {
        bootAudioGraph();
        if (!audioContext) {
            return;
        }
        if (audioContext.state === 'suspended') {
            await audioContext.resume();
        }
        cancelAnimationFrame(animationFrame);
        drawActive();
    });

    audio.addEventListener('pause', () => {
        cancelAnimationFrame(animationFrame);
        renderIdleLoop();
    });

    audio.addEventListener('ended', () => {
        cancelAnimationFrame(animationFrame);
        renderIdleLoop();
    });

    window.addEventListener('resize', () => {
        resizeCanvas();
        if (audio.paused) drawIdle();
    });

    document.addEventListener('visibilitychange', () => {
        cancelAnimationFrame(animationFrame);
        if (!document.hidden && audio.paused && isCanvasVisible) {
            renderIdleLoop();
        }
    });
}

const protectedDownloadButtons = document.querySelectorAll('[data-protected-file]');
const downloadModal = document.getElementById('snow-download-modal');
const downloadForm = document.getElementById('snow-download-form');
const downloadPassword = document.getElementById('snow-download-password');
const downloadTarget = document.getElementById('snow-download-target');
const downloadStatus = document.getElementById('snow-download-status');
const closeDownloadButtons = document.querySelectorAll('[data-snow-download-close]');

if (protectedDownloadButtons.length && downloadModal && downloadForm && downloadPassword) {
    let pendingFile = '';
    let lastTrigger = null;

    const downloadNames = {
        'full-score': 'snow-white-full-score.pdf',
        'lead-sheet': 'snow-white-lead-sheet.pdf',
        'vocal-score': 'snow-white-vocal-score.pdf',
        charts: 'snow-white-charts.pdf',
        'piano-arrangement': 'snow-white-piano-arrangement.pdf'
    };

    function openDownloadModal(button) {
        pendingFile = button.dataset.protectedFile || '';
        lastTrigger = button;
        if (downloadTarget) {
            downloadTarget.textContent = button.dataset.downloadLabel || 'Protected score file';
        }
        if (downloadStatus) {
            downloadStatus.textContent = '';
        }
        downloadPassword.value = '';
        downloadModal.hidden = false;
        downloadPassword.focus();
    }

    function closeDownloadModal() {
        downloadModal.hidden = true;
        pendingFile = '';
        downloadForm.reset();
        if (downloadStatus) {
            downloadStatus.textContent = '';
        }
        if (lastTrigger) {
            lastTrigger.focus();
        }
    }

    function saveBlob(blob, fileName) {
        const url = URL.createObjectURL(blob);
        const link = document.createElement('a');
        link.href = url;
        link.download = fileName;
        document.body.appendChild(link);
        link.click();
        link.remove();
        URL.revokeObjectURL(url);
    }

    protectedDownloadButtons.forEach((button) => {
        button.addEventListener('click', () => openDownloadModal(button));
    });

    closeDownloadButtons.forEach((button) => {
        button.addEventListener('click', closeDownloadModal);
    });

    document.addEventListener('keydown', (event) => {
        if (event.key === 'Escape' && !downloadModal.hidden) {
            closeDownloadModal();
        }
    });

    downloadForm.addEventListener('submit', async (event) => {
        event.preventDefault();
        if (!pendingFile) return;

        const submitButton = downloadForm.querySelector('button[type="submit"]');
        if (submitButton) {
            submitButton.disabled = true;
            submitButton.textContent = snowText('downloading');
        }
        if (downloadStatus) {
            downloadStatus.textContent = '';
        }

        try {
            const response = await fetch('/api/snow-white/download', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json'
                },
                body: JSON.stringify({
                    file: pendingFile,
                    password: downloadPassword.value
                })
            });

            if (!response.ok) {
                if (downloadStatus) {
                    downloadStatus.textContent = response.status === 401
                        ? snowText('incorrect')
                        : snowText('failed');
                }
                return;
            }

            const blob = await response.blob();
            saveBlob(blob, downloadNames[pendingFile] || 'snow-white-score.pdf');
            closeDownloadModal();
        } catch (_error) {
            if (downloadStatus) {
                downloadStatus.textContent = snowText('network');
            }
        } finally {
            if (submitButton) {
                submitButton.disabled = false;
                submitButton.textContent = snowText('submit');
            }
        }
    });
}
